## Notes

### `GRAMMAR.md`:

#### Лексика

1) Дополни документацию полным списоком всех ключевых слов вместо "и т.д."
2) Проверить определение типов как идентификаторов, а не ключевых слов
3) Доработай документацию, указать все арифметические операции. Доработай тест на все арифметические операции
4) Задокументировать логическое отрицание 
5) В документации не описано использование в языке квадратные скобок.
6) В документации не описано использование точки (обращение к полям структуры)
7) **Определены ли пространства имен и оператор его разрешения? Должно быть определено ,ибо это обязательное требование ТЗ**

#### Синтаксис

1) Уточни формулировку документации "Грамматика основана на рекурсивном спуске ДЛЯ ОПЕРАТОРОВ" Возможно "для объявлений и инструкций", а не "для операторов" уточни.
2) Уточни формулировку документации "и на Pratt парсере для математических выражений?" Возможно "выражений в целом"? Уточнить.
3) `stmt` должен быть вынесен на уровень выше, он не должен являться инстанцией `Decl`. Сдлей тест проверяющий это.
4) Должен поддерживаться прототип функции, чтобы удовлетворить требование ТЗ, чтобы в языке использовались объекты (переменные, функции) только после объявления. Сделать тест проверяющий это 
5) `Vardecl` допускает многомерные массивы. Учитывая, что в языке нет указателей, нельзя допускать многомерные массивы. Нужно оставить только одномерные массивы с простыми базовыми типами. Сделай тест проверяющий это.
6) `vardecl` допускает непроинициализированные переменные. Это должно быть запрещено. Сделай тест проверяющий это.
7) `Block` является блоком инструкций. На этом уровне должы быть запрещены все объявления, кроме объявления переменной. Блок должен содержать инструкции, а не объявления. Сделай тест проверяющий это.
8) для надежности запретить инструкции, являющиеся выраженимем. Определить надо только специальную инструкцию вызова функции в качестве возможной инстанции выражения. Сделай тест проверяющий это. 
9) Вызов всех встроенных функций должен быть определен одним грамматическим правилом: Например `Indentifier` "(" `Expr` ("," `Expr`)? ")"). Сделай тест проверяющий это
10) Список встроенных функций оформить отдельным документом. 
11) Для надежности присваивание должно являться исключительно инструкцией, и недопустимо как инстанция выражения. Сделай тест проверяющий это.
12) Документация: Литералы требуется объединить под общей граматической единицей, Не допускается каждый раз обращаться к каждому в остальных частях грамматики.

### Семантика

1) Проверить, допускается ли рекурсивное определение структуры уже далее в семантике, должно быть запрещено. Сделай тест проверяющий это.
2) Проверитиь, что инструкции управления циклом допускаются только внутри тела цикла (возможно кратно вложено в блоки, которые объемлются именно блоком цикла). Выполнить реализацию - depth guard. Сделай тест проверяющий это.

## Прочее

1) Добавить конструкцию приведения типа. `как<тип>(выражение)`. Определить семантикой ограничения на применение. В целом, в семантике языка должно быть сказано, что помимо запрета на неявное приведения типов, допускается только явно определенные приведения типов. Если констуркция `как` используется не по определенному случаю, то это статическая ошибка.