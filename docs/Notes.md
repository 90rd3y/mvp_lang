## Notes

1) Стоит ли допускать массив символов? Стоит ли удалить символы из языка, оставив только тип строка?
Идея: вырезать символьный тип, оставив только строковый.
2) Стоит ли допустить массиву и структуры быть вложенными одно в другое?
3) 
.) Имена функций из пространств имен разрешаются с помощью т.н. "полных имен". Тоже самое тогда ложно быть для структур и переменных. В целом ,проверить логику пространтсв имен в языке.
.) В интерпретации добавить проверку индексации массива, что число неотрицательно.
.) В интерпретации реализовать приведения между логическим, целым и плавающим типами.

## Todo

1) Обновить спецификацию языка в соответствии с текущим кодом (до семантики включительно).
2) Ревью интерпретации, поправить ошибки. Убрать всю ненужную сложность. Переписать так чтобы не было дублирования кода.
3) Написание тестов.
4) Сборка, тестирование, отладка.

## Допы
1) Стек вызовов (10)
2) Вложенные блочные комменты (5)
3) Data-oriented flat AST (20 + 5)
4) Интернированные типы (10)
5) Интернированные идентификаторы и строки (5)
6) Пратт парсер (5)
7) Established


